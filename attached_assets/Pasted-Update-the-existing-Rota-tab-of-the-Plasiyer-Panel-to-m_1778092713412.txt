Update the existing "Rota" tab of the Plasiyer Panel to match the provided reference image as closely as possible.

Important:
- Use the reference image as the primary UI guide.
- Keep the existing project structure, routing, and overall design system unless necessary for this screen update.
- Do not redesign the whole app.
- Only modernize and restructure the "Rota" page based on the screenshot.

GOAL:
Create a route dashboard screen for the field sales representative that visually matches the reference:
- left sidebar
- top search/header area
- page title and date summary
- orange/green progress summary bar
- separate pending and visited customer sections
- clean white cards with rounded corners
- soft shadows
- orange accents for pending items
- green accents for visited items

PAGE CONTENT:

1. Sidebar
- Keep the left navigation visible
- Highlight the "Rut" / "Rota" menu item as active
- Preserve the existing branding/logo area
- Preserve the bottom user/profile/logout area

2. Top Header
- Search input at the top
- Notification icon on the right
- User avatar and user info on the right

3. Main Page Header
- Title: "Bugünün Noktaları"
- Subtitle: current date and total point count
- Example format: "9 Mart Pazartesi - 6 nokta"

4. Route Progress Summary
Add a horizontal segmented summary bar:
- Left side orange: "Bekleyen: X Nokta"
- Right side green: "Gidilen: Y Nokta"

5. Pending Customers Section
Section title: "Bekleyen Noktalar"

Each row must include:
- route visit order number in an orange square badge
- customer name
- district / location text under customer name
- right arrow icon on the far right

Behavior:
- clicking a pending customer should navigate to the related order / visit detail screen

6. Visited Customers Section
Section title: "Gidilmiş Noktalar"

Each row must include:
- route visit order number in a green square badge
- customer name
- district / location text under customer name
- green check icon on the far right

Behavior:
- visited rows can be non-primary / read-only style
- if click is needed, only open details, not primary order entry

SORTING RULES:
- Customers must be listed strictly according to the sales representative’s predefined visit order
- Do not auto-sort dynamically
- Do not reorder by customer behavior, AI prediction, order probability, or missed-order logic
- Preserve the planned route sequence at all times
- Pending list: show only customers with status = pending, sorted by visit_order ascending
- Visited list: show only customers with status = visited, sorted by visit_order ascending

DATA EXPECTATION:
Each route item should support:
- id
- customer_name
- district
- visit_order
- status ("pending" | "visited")

The route day summary should support:
- date
- total_points
- pending_points
- visited_points

DESIGN REQUIREMENTS:
- modern SaaS dashboard look
- tablet-friendly responsive layout
- rounded cards
- soft shadow
- light gray page background
- white content cards
- orange for pending
- green for visited
- clean spacing and hierarchy
- truncate long customer names with ellipsis if needed

IMPLEMENTATION NOTES:
- Reuse existing components where possible
- If needed, create small reusable components for:
  - RouteSummaryBar
  - PendingRouteList
  - VisitedRouteList
  - RouteListItem
- Keep the code clean and production-oriented
- Do not add unnecessary features
- Focus on matching the reference layout closely